# Cleanup Prompt

Delete unwanted code and overlays. Do not add new codes. Do not change anything in the backend.

Match the web UI behavior and styling with the native apps. Do not add new code unless it is required to fix an existing bug. Prefer deleting duplicated, unused, or conflicting code and removing bad overrides.

Fix UI problems caused by pure black surfaces, heavy overlays, and solid-color buttons. Buttons should use the existing soft/shared button style instead of hard solid fills.

Before finishing, scan the affected frontend/native UI files fully and confirm the remaining code follows the existing shared source and style rules.
