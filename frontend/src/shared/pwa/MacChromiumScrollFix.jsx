export function MacChromiumScrollFix() {
  // Native browser scrolling is the smoothest path across Safari, Chrome, Edge,
  // and installed PWAs. This component stays mounted for compatibility only.
  return null;
}
